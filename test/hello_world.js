'use strict';

var expect = require('chai').expect;

describe('Extreme JavaScript Sandbox', function() {
  it('allows me to run tests', function() {
    expect('Hello World'.toLowerCase()).to.equal('hello world');
  });
});
